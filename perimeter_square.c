#include<stdio.h>
int main()
{
	int a,perimeter;
	printf("enter the one side length of a square:",a);
	scanf("%d",&a);
	perimeter=4*a;
	printf("the perimeter of a square is %d",perimeter);
	return 0;
}

